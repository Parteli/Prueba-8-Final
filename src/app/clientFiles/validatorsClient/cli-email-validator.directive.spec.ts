import { CliEmailValidatorDirective } from './cli-email-validator.directive';

describe('CliEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CliEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
