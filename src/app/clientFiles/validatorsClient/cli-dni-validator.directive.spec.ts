import { CliDniValidatorDirective } from './cli-dni-validator.directive';

describe('CliDniValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CliDniValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
