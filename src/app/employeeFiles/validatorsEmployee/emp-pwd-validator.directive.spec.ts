import { EmpPwdValidatorDirective } from './emp-pwd-validator.directive';

describe('EmpPwdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpPwdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
