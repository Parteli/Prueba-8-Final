import { EmpIdValidatorDirective } from './emp-id-validator.directive';

describe('EmpIdValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpIdValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
